<%- include('header', {title: `Home`}); -%>
  <body>
    
    <% sightings.forEach((element, index) => { %>
    <form action="/sighting/<%= index %>?_method=DELETE" method="POST">
    <a href="/sighting/<%= index %>">Sighting <%= index %></a>
    <input type="submit" value="Delete" />
    <br />
    </form>
    <% }); %>
    
  </body>
</html>