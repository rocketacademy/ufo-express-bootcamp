<%- include('header', {title: `Shapes`}); -%>
  
    <div>
    <% sightings.forEach((element, index) => { %>
    
    <a href="/sighting/<%= element.index %>">Sighting <%= element.index %></a>
    
    <br />
    
    <% }); %>
    </div>
  </body>
</html>